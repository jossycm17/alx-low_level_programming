#include <stdio.h>
/**
 * main - A program to prints the alphabet in lowercase, followed by a new line
 *
 * Return: 0 (Success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
